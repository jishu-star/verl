# Extra dependencies for the table reward. Install into the same environment that runs
# verl.trainer.main_ppo -- the reward executes inside the trainer process.
apted>=1.0.3      # tree edit distance for TEDS
lxml>=4.9         # HTML parsing; the recovering parser is what makes truncated tables scorable
